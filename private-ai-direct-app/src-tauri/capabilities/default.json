{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the direct-connect desktop app.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
